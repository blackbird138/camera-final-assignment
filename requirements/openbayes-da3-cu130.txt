# OpenBayes RTX 5090 environment for task A after switching to Depth Anything 3.
# Install from the project root with:
#   python -m pip install -r requirements/openbayes-da3-cu130.txt
#
# Then install the DA3 repo itself with:
#   cd Depth-Anything-3
#   python -m pip install --no-deps -e .
#
# We install DA3 with --no-deps because the upstream pyproject currently depends
# on opencv-python. OpenBayes containers may not provide libGL.so.1, so this file
# uses opencv-python-headless instead.

--index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://pypi.org/simple

torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
xformers

einops
huggingface-hub>=0.23,<1
imageio
numpy==1.26.4
opencv-python-headless==4.11.0.86
open3d
fastapi
uvicorn
requests
typer>=0.9.0
pillow>=10,<12
omegaconf
evo
e3nn
moviepy==1.0.3
plyfile
pillow-heif
safetensors
trimesh
pycolmap
